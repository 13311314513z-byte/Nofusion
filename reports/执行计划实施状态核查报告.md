# 执行计划实施状态核查报告

> 核查对象：`以Agent改进写作质量提升执行方案.md`（第三版）
> 核查方式：文件存在性 + 关键内容验证
> 生成日期：2026-06-14

---

## Stage 0：实施状态重置与意图系统收口

| Task | 预期交付物 | 状态 | 说明 |
|:----:|-----------|:----:|------|
| **Task 1**：建立 `AuthorChapterIntentSchema` Zod 校验 | 🆕 `packages/core/src/models/chapter-intent.schema.ts` | ✅ Done | 文件存在，包含 `AuthorChapterIntentSchema`、`AuthorScenePlanSchema`、`AuthorCharacterStateSchema`、`ChapterIntentsIndexSchema`，以及 `migrateLegacyIntent()` / `migrateIntentsIndex()` 迁移辅助函数。字段齐全（revision/status/updatedAt/source）。 |
| **Task 2**：合并 Core/Studio contract | Studio 移除独立接口，统一从 core 导入 | ✅ Done | `BookGoalsSection.tsx` 从 `@actalk/inkos-core` 导入 `AuthorChapterIntent`。Studio 侧未发现独立的 interface 定义。 |
| **Task 3**：决定 `IntentCommitment` 唯一事实源 | `chapter_intents.json` 为唯一写入源；SQLite 只存派生结果 | 🔶 Partial | `memory-db.ts` 中 `IntentCommitment` 接口含 `intentRevision` 字段，代码注释明确说明设计意图。**但 `addIntentCommitment()` / `verifyIntentCommitment()` 均无调用方**（仅定义，零调用）。文档要求的"删除未使用的方法或增加接线测试"未执行。 |
| **Task 4**：intent 增加 revision/status 追踪 | 每次修改 `revision += 1`；生成后标记 `confirmed`；旧版本标记 `superseded` | 🔶 Partial | `upsertChapterIntent()` 会递增 `revision`。**但旧版本未标记为 `superseded`**（只更新同一下标位置，旧数据直接覆盖）。生成后不会自动设 `status: "confirmed"`。 |
| **Task 5**：关键画面检查降级为 advisory | 输出级别从 `warning` 降为 `info`；标注 `heuristic` | ✅ Done | `validateAuthorIntentInContent()` 中 `keyMoment` 和 `coreNarrative` 的 `severity` 均为 `"info"`，注释写明"Downgraded from warning to info — heuristic check"。`readerTakeaway` 按设计不参与关键词检查。 |
| **Task 6**：P0 提醒默认不阻塞 | Studio 非模态提醒；CLI warning；`strictInterview: true` 才阻塞 | ❌ Missing | 未发现 `strictInterview` 在 Studio 或 CLI 中的实际阻塞逻辑实现。存在 `WritingConfigSchema.strictInterview` 定义，但 pipeline runner 未使用该字段。 |

### Stage 0 验收标准核查

| 标准 | 状态 |
|------|:----:|
| `AuthorChapterIntentSchema` 建立并通过 Zod 校验测试 | ✅ `schema.ts` 已定义，但未见独立 Zod 校验测试 |
| Core/Studio 共用同一份 contract | ✅ Studio 从 core 导入 |
| IntentCommitment 唯一事实源已确定，不存在双写路径 | 🔶 设计已确定，但 SQLite 方法无调用方，存在死代码 |
| intent 的 revision/status/updatedAt 正常递增 | 🔶 revision 递增，但 superseded 未实现 |
| 关键画面检查降级为 advisory | ✅ 已为 info |
| 自动写作不因未填写 intent 停摆 | 🔶 未发现阻塞逻辑，但 `strictInterview` 也未实际接线 |

---

## Stage 1：评测与运行追踪

| Task | 预期交付物 | 状态 | 说明 |
|:----:|-----------|:----:|------|
| **Task 1**：建立评测样本集 | 12-18 探索集 / 30+ 校准集 / 10-15 保留集 | 📋 数据任务 | 属于数据采集过程，非代码交付 |
| **Task 2**：建立人工成对偏好工具 | 🆕 `packages/core/src/evaluation/paired-preference.ts` | ✅ Done | 完整实现 `PairedPreference`、`PairedPreferenceQuestion`、`PreferenceMetrics` 接口，含 `computePreferenceMetrics()` 函数。 |
| | 🆕 `scripts/preference-eval.mjs` | ✅ Done | 完整实现：CSV 解析、Wilson 置信区间、Fleiss' Kappa 读者一致性、分维度胜率、Markdown 报告生成 |
| | 🆕 `scripts/evaluate-chapter.mjs` | ✅ Done | 完整实现：双盲 A/B 展示、5 维度问题（engagement/character/emotion/clarity/expectation）、随机顺序、CSV 输出 |
| **Task 3**：基线数据采集 | 记录 commit/model/tokens/延迟等 | 📋 数据任务 | 数据采集过程 |
| **Task 4**：产出基线报告 | 🆕 `reports/写作质量基线报告.md` | ❌ Missing | 文件不存在 |

### Stage 1 验收标准核查

| 标准 | 状态 |
|------|:----:|
| 探索集 ≥ 12 个任务 | 📋 需人工完成 |
| 校准集 ≥ 30 个 A/B 对 | 📋 需人工完成 |
| 保留集 ≥ 10 个任务 | 📋 需人工完成 |
| 成对偏好工具已实现 | ✅ 全部三个工具文件存在且功能完整 |
| 基线报告已生成 | ❌ 未生成 |
| 读者 ≥ 6 人，至少 3 种角色 | 📋 需人工组织 |
| 评测材料隐藏版本信息 | ✅ CLI 工具实现了随机盲化 |

---

## Stage 2：Prompt Manifest Lite

| Task | 预期交付物 | 状态 | 说明 |
|:----:|-----------|:----:|------|
| **Task 1**：定义 Fragment/Manifest 数据结构 | 🆕 `packages/core/src/models/prompt-manifest.ts` | ✅ Done | `PromptFragment`、`PromptManifest`、`BuildManifestInput` 接口齐全，`buildPromptManifest()` 含优先级排序和超预算丢弃逻辑 |
| **Task 2**：Planner/Writer/Auditor 接入 | 三个 Agent 接入 Manifest | ✅ Done | `planner.ts`、`writer.ts`（3 处）、`continuity.ts` 均导入并使用 `logPromptManifest()`。Agent 中调用 manifest 的位置：`planner:358`、`writer:290/602/654`、`continuity:651`。 |
| **Task 3**：Token 预算计算工具 | `getAvailableInputTokens()` | ✅ Done | 函数实现在 `prompt-manifest.ts`，含模型上下文窗口映射、协议开销和安全余量。被 `prompt-tracing.ts` 使用。 |
| **Task 4**：新组件接入规范 | 开发规范文档 | ❌ Missing | 未在仓库中发现相关规范文档 |

### Stage 2 验收标准核查

| 标准 | 状态 |
|------|:----:|
| 接入前后 prompt hash 一致 | 🔶 已实现 hash 计算能力，但未见接入前后的快照对比测试 |
| Manifest 可解释每个片段来源和 Token | ✅ `PromptFragment` 含 source、estimatedTokens |
| 超预算时 dropped fragments 可追踪 | ✅ `PromptManifest.droppedFragments` 已实现 |
| Planner/Writer/Auditor 已接入 | ✅ 均已使用 `logPromptManifest` |
| 新组件接入规范已写入 | ❌ 未写入 |

---

## Stage 3：确定性质量增强

| Task | 预期交付物 | 状态 | 说明 |
|:----:|-----------|:----:|------|
| **Task 1**：Genre Profile 增强 | `GenreProfile.promises` 字段 | ✅ Done | `GenreProfileSchema` 含 `promises` 数组字段（`id`/`description`/`importance`/`scope`/`expectedWindow`/`overduePolicy` 等）。另有 `evaluation/genre-promises.ts` 实现 `checkGenrePromises()` 和 `getCriticalGenrePromises()`。**但未接入 pipeline runner**。 |
| **Task 2**：统一 `AuditIssue` Schema | 🆕 `packages/core/src/models/audit-issue.ts` | ✅ / 🔶 | 统一 Schema 已定义（含 `id`/`source`/`severity`/`location`/`confidence`/`fixScope`/`blocking`/`createdAt`），并有 `fromLegacyContinuityIssue()` 转换函数。**但 `continuity.ts` 仍保留独立的简化版 `AuditIssue` 接口**（只含 severity/category/description/suggestion），pipeline 中仍沿用此旧类型。 |
| **Task 3**：Issue Normalizer | 🆕 `packages/core/src/agents/issue-normalizer.ts` | ✅ Done | `IssueNormalizer` 类完整实现：category 标准化、精确去重、Levenshtein 近似匹配、跨章 severity 升级、按 fixScope 分组。**但未接入 pipeline runner**。 |
| **Task 4**：强化 Reviser patch 模式 | `selectReviseMode()` + patch 边界检查 | 🔶 Partial | `utils/patch-boundary.ts` 已实现 `selectReviseModeFromFixScope()` 和 `checkPatchBoundary()`，并从 `index.ts` 导出。**但未在 `reviser.ts` 或 `chapter-review-cycle.ts` 中使用**。Reviser 使用自有的 `resolveAutoOutputMode()`（基于关键词模式匹配而非 fixScope）。 |
| **Task 5**：增加 `WritingConfigSchema` | 🆕 `packages/core/src/config/writing-config.ts` | ⚠️ 重复定义 | `config/writing-config.ts` 完整定义了 `WritingConfigSchema`（`qualityBudget`/`strictInterview`/`betaReaderMode`/`keySceneCandidates`/`reviewRetries`）。**但 `models/project.ts` 中也有一份完全相同的独立定义**（不是从 config 导入），造成两份定义不同步的风险。**pipeline runner 未引用 `WritingConfigSchema`**（无 `qualityBudget` 或 `betaReaderMode` 读取）。 |

### Stage 3 验收标准核查

| 标准 | 状态 |
|------|:----:|
| GenreProfile 扩展完成，不依赖关键词 | ✅ `promises` 字段已加入，校验不依赖关键词 |
| `AuditIssue` Schema 扩展完成 | ✅ 统一模型有 location/confidence/fixScope |
| Issue Normalizer 可在 category+location 维度去重 | ✅ 已实现 |
| Reviser 根据 fixScope 自动选择最小修改范围 | ❌ 未接入（reviser 仍用自有路由逻辑） |
| 检测 patch 越界修改 | ❌ `checkPatchBoundary` 已实现但未接线 |
| WritingConfigSchema 就绪并接入 | ⚠️ 就绪但未接入，且有两份重复定义 |

---

## Stage 4：Beta Reader 影子评测

| Task | 预期交付物 | 状态 | 说明 |
|:----:|-----------|:----:|------|
| **Task 1**：证据型读者模拟器 | 🔧 `packages/core/src/agents/beta-reader.ts` | ✅ Agent 存在 / ❌ 未接入 | `BetaReader` 类完整实现（继承 `BaseAgent`），含 `read()` 方法。输出 `BetaReaderOutput`（`observations` + `modelInfo`）。**但 pipeline runner 中无任何调用**。 |
| | 🆕 `packages/core/src/models/beta-reader-output.ts` | ✅ Done | `ReaderObservation`、`BetaReaderOutput`、`BetaReaderMode` 类型均已定义，含 `summarizeObservations()`。 |
| **Task 2**：三步成熟度模式 | `BetaReaderMode` 类型 | ✅ Done | `type BetaReaderMode = "off" | "shadow" | "advisory" | "actionable"` 已定义 |
| **Task 3**：校准流程 | 30+ 配对样本对比 | 📋 数据任务 | 流程任务，非代码交付 |

### Stage 4 验收标准核查

| 标准 | 状态 |
|------|:----:|
| Beta Reader 输出证据型 ReaderObservation | ✅ 每个 observation 含 paragraph 级别的 evidence |
| Writer 与 Reader 使用不同模型家族 | 🔶 类型系统支持，但未验证实际部署配置 |
| 记录 Reader 模型、prompt hash、版本 | ✅ `modelInfo` 字段包含 |
| 校准集 ≥ 30 配对样本 | 📋 需人工完成 |
| 默认模式为 shadow | ✅ `WritingConfigSchema` 默认为 `"off"`（文档要求 shadow，配置是 off） |
| 影子评测失败不改变最终章节 | 🔶 因未接入 pipeline，尚无法验证 |

---

## Stage 5：数据驱动选择专项质量改进

**状态**：📋 数据驱动流程 — 无代码交付物可核查。需等待 Stage 1 基线和 Stage 4 数据完成后选择方向。

---

## Stage 6：高成本架构（按需立项）

**状态**：📋 按需启动 — 当前无 Stage 1-5 数据支撑，未启动。

---

## 附录 A：文件变更总表对照

| 操作 | 文件路径 | Stage | 实际状态 |
|:----:|---------|:-----:|:--------:|
| 🆕 | `packages/core/src/models/chapter-intent.schema.ts` | 0 | ✅ 存在，内容完整 |
| 🔧 | `packages/core/src/models/chapter-intent.ts` | 0, 5 | ✅ 已修改，增加 revision/status/updatedAt/source，旧数据迁移函数，但 superseded 未实现 |
| 🔧 | `packages/core/src/agents/post-write-validator.ts` | 0, 3 | ✅ 关键画面检查 severity 已改为 info |
| 🔧 | `packages/core/src/state/memory-db.ts` | 0 | ✅ IntentCommitment 接口含 intentRevision，但方法无调用方 |
| 🔧 | `packages/studio/src/pages/book-workspace/*.tsx` | 0 | ✅ 从 core 导入类型，无独立接口 |
| 🆕 | `packages/core/src/evaluation/paired-preference.ts` | 1 | ✅ 存在，内容完整 |
| 🆕 | `scripts/preference-eval.mjs` | 1 | ✅ 存在，功能完整 |
| 🆕 | `scripts/evaluate-chapter.mjs` | 1 | ✅ 存在，功能完整 |
| 🆕 | `packages/core/src/models/prompt-manifest.ts` | 2 | ✅ 存在，内容完整 |
| 🔧 | `packages/core/src/agents/planner.ts` | 2 | ✅ 已接入 logPromptManifest |
| 🔧 | `packages/core/src/agents/writer.ts` | 2 | ✅ 已接入 logPromptManifest（3处） |
| 🔧 | `packages/core/src/agents/continuity.ts` | 2 | ✅ 已接入 logPromptManifest |
| 🔧 | `packages/core/src/models/genre-profile.ts` | 3 | ✅ promises 字段已加入 |
| 🆕 | `packages/core/src/models/audit-issue.ts` | 3 | ✅ 统一 Schema 已定义 |
| 🆕 | `packages/core/src/agents/issue-normalizer.ts` | 3 | ✅ 存在，内容完整 |
| 🔧 | `packages/core/src/agents/reviser.ts` | 3 | 🔶 已有 spot-fix/patch/rewrite 路由，但未接入 fixScope 驱动的选择 |
| 🆕 | `packages/core/src/config/writing-config.ts` | 3 | ⚠️ 存在，但 project.ts 中有重复定义 |
| 🔧 | `packages/core/src/agents/beta-reader.ts` | 4 | ✅ Agent 存在，但未接入 pipeline |
| 🆕 | `packages/core/src/models/beta-reader-output.ts` | 4 | ✅ 存在，内容完整 |
| 🔧 | `packages/core/src/pipeline/runner.ts` | 0, 3, 4 | 🔶 已接 `validateAuthorIntentInContent`，但未接 WritingConfig/BetaReader/genre-promises/IssueNormalizer |

---

## 核心发现汇总

### 已完整实现的组件（✅）
1. **Stage 0**: `AuthorChapterIntentSchema` Zod schema、Core/Studio contract 合并、关键画面检查 advisory 降级
2. **Stage 1**: 成对偏好评测全套工具（`paired-preference.ts` + `preference-eval.mjs` + `evaluate-chapter.mjs`）
3. **Stage 2**: Prompt Manifest 数据结构、Token 预算计算、三 Agent Manifest 接入
4. **Stage 3**: GenreProfile promises 字段、统一 AuditIssue 模型、IssueNormalizer、WritingConfigSchema（有重复）
5. **Stage 4**: BetaReader Agent 类、BetaReaderOutput 模型、三步成熟度模式

### 已实现但未接入 Pipeline 的组件（🔶）
1. **BetaReader**: Agent 完整但 runner.ts 未调用
2. **IssueNormalizer**: 完整但未在 pipeline 中使用
3. **checkGenrePromises**: 完整但 runner.ts 未调用
4. **selectReviseModeFromFixScope / checkPatchBoundary**: 已实现未接线
5. **WritingConfig / qualityBudget**: Schema 存在但 runner.ts 未读取

### 部分实现（🔶）
1. **IntentCommitment 事实源**: 设计已定，SQLite 方法零调用
2. **Revision/Superseded 追踪**: revision 递增，但 superseded 状态和 auto-confirmed 未实现
3. **Reviser fixScope 驱动**: 路由逻辑已定义但未接入

### 完全缺失（❌）
1. **Stage 0 Task 6**: P0 提醒默认不阻塞（`strictInterview` 阻塞逻辑）
2. **Stage 1 Task 4**: `reports/写作质量基线报告.md` 基线报告
3. **Stage 2 Task 4**: 新组件接入规范文档

### 代码质量问题（⚠️）
1. **WritingConfigSchema 重复定义**: `config/writing-config.ts` 和 `models/project.ts` 各有一份
2. **AuditIssue 双类型**: `continuity.ts` 自有简化版 + `models/audit-issue.ts` 统一版并存
3. **IntentCommitment 死代码**: 4 个方法定义但零调用

### 数据/流程任务（📋）
- Stage 1 样本集采集、基线报告撰写、读者组织
- Stage 4 校准流程
- Stage 5 全流程

---

## 推荐优先解决项

1. **P0**: 清除 `WritingConfigSchema` 重复定义，`project.ts` 改为从 `config/writing-config.ts` 导入
2. **P0**: 将 `WritingConfig` 接入 pipeline runner，至少实现 `qualityBudget` 和 `betaReaderMode` 读取
3. **P1**: 将 BetaReader 接入 pipeline runner（初期 shadow 模式）
4. **P1**: 清理 `IntentCommitment` 死代码 — 确认是否需保留，否则删除
5. **P1**: 实现 `superseded` 状态标记和 auto-confirmed 逻辑
6. **P2**: 将 `selectReviseModeFromFixScope` 接入 reviser 路由
7. **P2**: 将 `checkGenrePromises` 接入 pipeline audit 流程
8. **P2**: 将 `IssueNormalizer` 接入 pipeline 审校后处理
