# 0621 全量测试验收报告 GPT

> 生成时间：2026-06-21 18:55（Asia/Shanghai）  
> 工作区：`c:\Users\white\Downloads\Nofusion-main`  
> 方法：按根 `package.json` 交付链路与 Studio budget 校验逐项执行；以本次终端实测结果为唯一验收依据。  
> 结论：**未通过全量验收**。TypeScript、lint、build、Core tests、Studio tests、publish manifest、临时 budget 均通过；**CLI tests 失败 5/169**，且 bundle budget 仍依赖 16500KB 临时阈值。

## 一、测试环境与基线

| 项目 | 实测值 |
|---|---|
| Git branch | `master` |
| Git commit | `67e1ea9` |
| Node.js | `v24.14.0` |
| pnpm | `11.5.2` |
| 工作区状态 | `git status --short` 约 87 项改动/未跟踪文件 |
| 真实 LLM E2E | 未执行：`INKOS_RUN_REAL_LLM_E2E=false`，`OPENAI_API_KEY=false`，`ANTHROPIC_API_KEY=false`，`DEEPSEEK_API_KEY=false` |

[COMPUTED] 当前工作区不是干净基线。测试结果可验证当前本地状态，但不能直接等同于可复现 release 基线。

## 二、官方脚本口径

根 `package.json` 定义的主要交付链路：

```powershell
.\pnpm.cmd -r typecheck
.\pnpm.cmd -r lint
.\pnpm.cmd -r build
.\pnpm.cmd -r --workspace-concurrency=1 test
```

补充验收项：

```powershell
.\pnpm.cmd --filter @actalk/inkos-studio build:budget
.\pnpm.cmd run verify:publish-manifests
```

[COMPUTED] 本次实际采用逐项执行，而非完全依赖根级 `test:ci`，原因是根级 `test:ci` 曾在 184 秒后超时，且超时后遗留 Vitest worker。逐项执行可给出更清晰、可定位的失败面。

## 三、全量门禁结果

| 序号 | 命令 | 结果 | 关键输出 / 证据 |
|:--:|---|---|---|
| 1 | `.\pnpm.cmd -r typecheck` | ✅ 通过 | Core、Studio、CLI 均 `Done`，退出码 0 |
| 2 | `.\pnpm.cmd -r lint` | ✅ 通过 | 0 errors；Core 140 warnings，Studio 273 warnings，CLI 17 warnings |
| 3 | `.\pnpm.cmd -r build` | ✅ 通过 | Core build、Studio client/server/build verify、CLI build 均完成；Studio `Build artifacts verified` |
| 4 | `.\pnpm.cmd --filter @actalk/inkos-core test` | ✅ 通过 | 145 files / 1454 tests passed，Duration 19.00s |
| 5 | `.\pnpm.cmd --filter @actalk/inkos-studio test` | ✅ 通过 | 25 files / 277 tests passed，Duration 8.78s |
| 6 | `.\pnpm.cmd --filter @actalk/inkos test` | ❌ 失败 | 34 files 中 1 failed；169 tests 中 5 failed / 164 passed |
| 7 | `.\pnpm.cmd --filter @actalk/inkos-studio build:budget` | 🟡 临时通过 | Dist 16281.7KB / 16500KB；若按原 15500KB 目标仍超标 781.7KB |
| 8 | `.\pnpm.cmd run verify:publish-manifests` | ✅ 通过 | core / cli / studio 均 `OK` |
| 9 | `.\pnpm.cmd run test:ci` | ⚪ 未形成有效结论 | 184 秒超时；后续以逐项门禁结果为准 |

## 四、失败项详解

### 4.1 CLI tests 未通过

命令：

```powershell
.\pnpm.cmd --filter @actalk/inkos test
```

结果：

| 指标 | 数值 |
|---|---:|
| Test Files | 1 failed / 33 passed / 34 total |
| Tests | 5 failed / 164 passed / 169 total |
| Duration | 91.33s |
| 失败文件 | `packages/cli/src/__tests__/cli-integration.test.ts` |

关键失败证据：

```text
Error: Cannot find module 'C:\Users\white\Downloads\Nofusion-main\packages\cli\dist\index.js'
```

受影响用例至少包括：

| 用例 | 失败点 |
|---|---|
| `inkos book create > removes stale incomplete book directories before retrying create` | stderr 未包含预期 `Failed to create book`，实际为 Node module not found |
| `inkos status > shows project status with zero books` | 执行 `node packages/cli/dist/index.js status` 时找不到入口 |
| `inkos status > returns JSON with --json flag` | 同上 |

[COMPUTED] 测试结束后复查 `packages/cli/dist/index.js`，文件存在且 LastWriteTime 为 18:48:03。由此推断：失败不是源码永久缺少 dist，而更像 CLI integration 测试与 build/dist 清理、测试并发、或临时 pnpm 执行目录之间存在时序问题。

补充观察：

- CLI test 返回失败后仍遗留 Vitest / tinypool worker，需要手工清理。
- 根级 `test:ci` 超时也曾遗留 worker，说明 CLI/全量 test 的进程收敛需要单独修。

### 4.2 Bundle budget 仅临时通过

命令：

```powershell
.\pnpm.cmd --filter @actalk/inkos-studio build:budget
```

输出：

| 项目 | 实测 | 当前上限 | 状态 |
|---|---:|---:|---|
| JS | 15659.6KB | 18000KB | ✅ |
| CSS | 155.8KB | 300KB | ✅ |
| Max chunk | 1644.7KB | 1700KB | ✅ |
| Dist | 16281.7KB | 16500KB | ✅ 临时通过 |

[COMPUTED] 当前 `packages/studio/scripts/bundle-budget.mjs` 使用 `maxTotalDist: 16_500_000`，并注释为临时阈值。若按原 15500KB 目标，Dist 仍超标约 781.7KB。Vite build 中 `vendor-streamdown` 仍约 1616.65KB，是后续优化重点。

## 五、通过项说明

### 5.1 Typecheck

`.\pnpm.cmd -r typecheck` 当前已恢复通过：

```text
packages/core typecheck: Done
packages/studio typecheck: Done
packages/cli typecheck: Done
EXIT:0
```

[COMPUTED] 早先 `routes/style.ts` 的 `withPipeline` 未定义问题在本次最终复验前已不再复现；当前文件存在 `import { withPipeline } from "../shared/pipeline.js";`。

### 5.2 Lint

当前 lint 无 error，但 warning 仍多：

| 包 | Errors | Warnings |
|---|---:|---:|
| Core | 0 | 140 |
| Studio | 0 | 273 |
| CLI | 0 | 17 |

[COMPUTED] lint 不阻断验收，但 warnings 主要集中在 `no-unused-vars` 与 `no-explicit-any`，后者与 P2/P3 的 any 收敛目标一致。

### 5.3 Build

`.\pnpm.cmd -r build` 通过：

- Core build：通过。
- Studio build：client build、server build、build verify 均通过。
- CLI build：通过。

Studio build 仍有大 chunk 风险：

| Chunk | 体积 |
|---|---:|
| `vendor-streamdown-*.js` | 1616.65KB |
| `index-*.js` | 836.09KB |
| `emacs-lisp-*.js` | 779.85KB |
| `cpp-*.js` | 626.08KB |
| `wasm-*.js` | 622.34KB |

### 5.4 Core tests

```text
Test Files 145 passed (145)
Tests      1454 passed (1454)
Duration   19.00s
```

[COMPUTED] Core 测试全绿。测试过程中出现若干 `[composer] Failed to read ... ENOENT` stderr，但未导致测试失败；这些是缺省 truth/story 文件 fallback 场景的噪音，建议后续降噪或在测试中显式断言。

### 5.5 Studio tests

```text
Test Files 25 passed (25)
Tests      277 passed (277)
Duration   8.78s
```

[COMPUTED] Studio 测试全绿。`server.test.ts` 当前 95 tests 全部通过。stderr 中 `Agent error: boom` 来自错误路径测试，不构成失败。

### 5.6 Publish manifest

```text
OK: packages/core
OK: packages/cli
OK: packages/studio
```

[COMPUTED] 发布 manifest 中未发现 workspace protocol 泄漏。

## 六、静态质量指标

| 指标 | 当前值 | 验收含义 |
|---|---:|---|
| `packages/core/src/pipeline/runner.ts` | 2552 行 | 仍未达 `<1200` / facade 目标 |
| `packages/studio/src/api/server.ts` | 1233 行 | 接近 `<1200`，仍略超 |
| `packages/core/src/__tests__/pipeline-runner.test.ts` | 5192 行 | 巨型测试未拆 |
| `packages/studio/src/api/server.test.ts` | 3810 行 | 巨型测试未拆 |
| Core test files present | 146 | 实际执行 145；真实 LLM E2E 未默认执行 |
| Studio test files | 25 | 全部执行通过 |
| CLI test files present | 35 | 实际执行 34；1 file failed |
| 显式 any 总计 | 302 | 未达 `<50` 目标 |
| 非测试显式 any | 111 | 生产代码仍需收敛 |

## 七、验收裁决

| 维度 | 裁决 | 原因 |
|---|---|---|
| 最小功能门禁 | ❌ 未通过 | CLI tests 5 failed |
| Core 稳定性 | ✅ 通过 | typecheck/build/test 均通过 |
| Studio 稳定性 | ✅ 通过 | typecheck/build/test 均通过 |
| CLI 稳定性 | ❌ 未通过 | integration test 找不到 `dist/index.js` |
| Bundle 预算 | 🟡 临时通过 | 依赖 16500KB 临时阈值，未回到 15500KB |
| 发布 manifest | ✅ 通过 | verify script 全绿 |
| 质量债目标 | ❌ 未达 | runner/test 文件过大，any 未达目标 |

**最终结论**：当前项目不应标记为“全量测试验收通过”。更准确的状态是：

> **Core + Studio 主链路已通过；CLI integration 阻断全量验收；bundle budget 为临时豁免；P2/P3 质量目标仍未完成。**

## 八、优先修复方案

### 8.1 P0：修复 CLI tests 阻断

目标：`.\pnpm.cmd --filter @actalk/inkos test` 达到 169/169。

建议顺序：

1. 在 `packages/cli/src/__tests__/cli-integration.test.ts` 的 `beforeAll` 开始处断言 `cliEntry` 存在，若不存在直接输出当前 cwd、`cliDir`、`dist` 列表。
2. 检查同一 test run 内是否有其它 CLI 测试或 publish 测试清理 `packages/cli/dist`。
3. 将调用真实 dist 的 integration tests 与会触发 build/prepack/postpack 的测试隔离，必要时让 integration tests 使用独立临时 copy 的 CLI dist。
4. 修复后复跑：

```powershell
.\pnpm.cmd --filter @actalk/inkos build
.\pnpm.cmd --filter @actalk/inkos test
.\pnpm.cmd run test:ci
```

### 8.2 P0/P1：恢复预算真实目标

目标：Dist 回到 15500KB 以内，或明确发布前豁免。

建议顺序：

1. 拆 `vendor-streamdown` / Shiki 相关重型语言包，确认只按需加载。
2. 复查 `use-api.ts` 静态/动态混用是否导致懒加载失效。
3. 将 `maxTotalDist` 从 16500KB 恢复 15500KB 后复跑：

```powershell
.\pnpm.cmd --filter @actalk/inkos-studio build
.\pnpm.cmd --filter @actalk/inkos-studio build:budget
```

### 8.3 P2/P3：质量债后续验收项

不阻断当前 CLI 修复，但阻断“质量达标”结论：

| 项目 | 当前 | 目标 |
|---|---:|---:|
| `runner.ts` | 2552 行 | `<1200`，最终 facade |
| `pipeline-runner.test.ts` | 5192 行 | 拆成 6 文件，单文件 `<1500` |
| `server.test.ts` | 3810 行 | 拆成 5 文件，单文件 `<1500` |
| 非测试显式 any | 111 | `<50` |

## 九、复验命令清单

修复 CLI 阻断后，按以下顺序复验：

```powershell
.\pnpm.cmd -r typecheck
.\pnpm.cmd -r lint
.\pnpm.cmd -r build
.\pnpm.cmd --filter @actalk/inkos-core test
.\pnpm.cmd --filter @actalk/inkos-studio test
.\pnpm.cmd --filter @actalk/inkos test
.\pnpm.cmd --filter @actalk/inkos-studio build:budget
.\pnpm.cmd run verify:publish-manifests
.\pnpm.cmd run test:ci
```

真实 LLM E2E 仅在密钥与显式开关具备时执行：

```powershell
$env:INKOS_RUN_REAL_LLM_E2E='1'
.\pnpm.cmd run test:real-e2e
```

## 十、报告完整性说明

- [COMPUTED] 本报告没有修改项目源码，仅新增本报告文件。
- [COMPUTED] 本报告未把真实 LLM E2E 计入默认门禁，因为当前环境没有 opt-in 开关与 API key。
- [COMPUTED] 根级 `test:ci` 超时不作为单独失败根因；实际失败以可定位的 CLI test failure 为准。
- [COMPUTED] CLI test 和超时 test:ci 曾遗留 Vitest/tinypool worker；已按命令行识别并清理，仅保留原本存在的 Studio dev server 进程。
