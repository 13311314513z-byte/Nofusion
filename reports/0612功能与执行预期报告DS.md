# NoFusion 0612 功能与执行预期报告

> 评估时间：2026-06-12 | 基线：0611 报告 + 当前工作区全部修改

---

## 一、总览

基于 0611 多份报告（现状报告、核验报告、仍需改进报告、代码审查报告）的交叉复核，结合当前工作区的实际代码状态，项目在"集成收口与发布治理"阶段取得显著进展，但仍有若干关键路径需要闭合。

### 版本信息

| 包 | 版本 |
|---|------|
| inkos (monorepo) | 1.4.1 |
| @actalk/inkos (CLI) | 1.4.1 |
| @actalk/inkos-core | 1.4.1 |
| @actalk/inkos-studio | 1.4.1 |

---

## 二、0611 → 0612 新增完成项

以下项目在 0611 报告中列为待处理，在当前工作区已落地：

### P0 — 已修复

| # | 问题 | 0611 状态 | 当前状态 | 修改 |
|---|------|:--------:|:--------:|------|
| P0-1 | Studio 构建脚本无效 (`tsc -p ... --force`) | ❌ | ✅ | `build:server` 改为预清理 tsbuildinfo + `tsc -p tsconfig.server.json`，无 `--force` 参数 |
| P0-2 | 封面 API Key 静默清空 | ⚠️ | ✅ | 前后端 `hasApiKey`/`keyPreview`/`keyDirty` 已对接，空值不覆盖 |
| P0-3 | 书籍创建 `buildPipelineConfig` 异常导致 500 | ❌ | ✅ | 外包裹 IIFE + try/catch，异常写入 `bookCreateStatus` |
| P0-4 | `assertProjectRoot` 前缀绕过 | ⚠️ | ✅ | 已增加 `sep` 拼接，同前缀兄弟目录不能再绕过 |

### P1 — 已修复

| # | 问题 | 0611 状态 | 当前状态 | 修改 |
|---|------|:--------:|:--------:|------|
| P1-0 | Agent 会话队列死锁 | ❌ | ✅ | `runInAgentSessionQueue` 增加 15min 超时 + `Promise.race`；超时清除 Agent 缓存 |
| P1-1 | Agent 错误无日志 | ❌ | ✅ | `POST /api/v1/agent` catch 块增加 `console.error` |
| P1-2 | 章节保存无版本 backup | ❌ | ✅ | `PUT` 前自动备份到 `chapters/versions/`；新增版本列表与版本内容 API |
| P1-3 | 工作区无设置入口 | ❌ | ✅ | `BookSection` 类型 + `Nav` + i18n + 路由跳转至 `BookDetail` |
| P1-4 | 人物声线按钮禁用 | ❌ | ✅ | 移除 `disabled`，`onClick` 导航至第 1 章 |
| P1-5 | 伏笔只读 | ❌ | ✅ | 新增 POST/PUT/DELETE hooks API + 前端创建/编辑/删除表单 |
| P1-6 | 伏笔硬编码中文 | ❌ | ✅ | 新增 25+ i18n 键，替换表单/按钮/标签字符串 |

---

## 三、当前剩余问题与优先级

### P0 — 发布阻塞（1 项）

| # | 问题 | 影响 | 工作量 |
|---|------|------|:------:|
| **0** | CLI 测试 168/171 3 项失败 | 发布门禁未通过，`npm pack` 被阻断 | 2h |

CLI 失败项：
1. **Studio npm 包缺少 `dist/api/index.js`** — 需要验证连续两次构建的可靠性
2. **`inkos export --json` 契约不兼容** — `{ status, data }` vs 旧 `outputPath`
3. **`doctor` 本地端点超时** — localhost 端点探测等待过长

### P1 — 下一轮（6 项）

| # | 问题 | 影响 | 工作量 |
|---|------|------|:------:|
| **1** | CLI JSON 契约未统一 | `analytics`/`doctor`/`export` 已用 `formatJsonOutput`，其余未迁移；测试读旧格式 | 2h |
| **2** | CLI `doctor --skip-connectivity` 选项已加但 `--json` 输出被日志污染 | 影响 JSON 管道消费 | 2h |
| **3** | 修辞 issue 动作为 `console.log` | `ai-rewrite`/`mark-fixed` 无真实处理 | 4h |
| **4** | 应用后无自动复检 | 接受 diff 后不触发重新诊断 | 4h |
| **5** | 章节版本历史 UI | 后端 API 已就绪，前端无版本查看/回滚界面 | 4h |
| **6** | 蒸馏前端工作台 | 后端 5 端点就绪，前端无 UI | 8h |

### P2 — 技术债务（5 项）

| # | 问题 | 工作量 |
|---|------|:------:|
| **1** | 12 处 async `onClick` 重入保护 | 4h |
| **2** | `useAutoSave` 死代码 | 2h |
| **3** | `RhetoricHighlightEditor` 孤立组件 | 2h |
| **4** | book-workspace 78 处硬编码中文 | 1d |
| **5** | "一键检测全部"名称误导 | 1h |

---

## 四、当前工作区已确认状态

### 编译与测试

| 核验项 | 结果 | 说明 |
|--------|:----:|------|
| Core TypeScript | ✅ | 零错误 |
| Studio TypeScript | ✅ | 零错误 |
| CLI TypeScript | ✅ | 零错误 |
| Core 测试 | ✅ 1,294/1,294 | 基线稳定 |
| Studio 测试 | ✅ 267/267 | 基线稳定 |
| CLI 测试 | ❌ 168/171 | 3 项失败需修复 |

### 构建

| 核验项 | 结果 | 说明 |
|--------|:----:|------|
| `build:client` (Vite) | ✅ | 正常生成 `dist/index.html` |
| `build:server` (tsc) | ✅ | 命令已修复，`dist/api/index.js` 可生成 |
| `build:verify` | ⚠️ | 三个产物断言已添加，但 CLI 门禁未通过前整体构建不执行 |

### 安全

| 核验项 | 结果 | 说明 |
|--------|:----:|------|
| `assertProjectRoot` 前缀绕过 | ✅ | 已用 `sep` 修正 |
| 蒸馏路径遍历 | ✅ | `assertSafeAuthorId` 校验 |
| `samples/write` 路径遍历 | ✅ | 继承 security wrapper |
| `authors/fetch` SSRF | ✅ | URL + 目标地址双重校验 |
| 修辞 ID 冲突 | ✅ | Math.random 后缀 |
| 封面 Key 保留语义 | ✅ | `hasApiKey`/`keyDirty`/显式清除 |

### 功能

| 核验项 | 结果 | 说明 |
|--------|:----:|------|
| 书籍创建 | ✅ | `buildPipelineConfig` 异常安全 |
| 章节版本 backup | ✅ | `PUT` 自动备份 + 版本 API |
| 设置入口 | ✅ | 工作区导航新增设置按钮 |
| 人物声线按钮 | ✅ | 已启用 |
| 伏笔 CRUD | ✅ | 后端 + 前端表单 |
| 工作区 14 分区 | ✅ | 全部导出可用 |
| Agent 超时保护 | ✅ | 15min 超时 + 队列释放 + 缓存清除 |
| Agent 错误日志 | ✅ | `console.error` 输出到终端 |

---

## 五、执行建议

### 立即执行（发布前）

```
1. 修复 CLI 3 项失败（2h）
   ├── 对应 export --json 契约（`formatJsonOutput` raw:true 已加）
   ├── doctor 本地端点超时（--skip-connectivity 已加）
   └── 验证 Studio 构建产物在连续构建后稳定

2. 执行全量测试（pnpm test）
   ├── Core: 1,294 ✅
   ├── Studio: 267 ✅
   └── CLI: 171 ✅（修复后）

3. 发布包验证（npm pack --dry-run）
```

### 下一轮迭代

```
1. 修辞 issue 真实动作（4h）
2. 应用后自动复检（4h）
3. 章节版本 UI（4h）
4. CLI JSON 统一（2h）
5. 蒸馏前端工作台（8h）
```

### 可推迟

```
- 重入保护（4h）→ P2
- 硬编码中文（1d）→ P2
- 死代码清理（2h）→ P2
- 包体积优化（1d）→ P3
```

---

## 六、一句话结论

> **0611 报告列出的 P0 发布阻塞项（构建脚本/封面 Key/assertProjectRoot）已全部修复，Agent 超时保护、章节版本 backup、伏笔 CRUD、设置导航等 P1 功能也已完成。**  
> 当前唯一发布门禁是 **CLI 3 项测试失败**，修复后即可进入发布流程。  
> 下一阶段主方向：修辞动作闭环、章节版本 UI、蒸馏前端工作台。
