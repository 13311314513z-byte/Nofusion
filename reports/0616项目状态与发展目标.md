# NoFusion / InkOS 项目当前状态汇报、推进建议与进一步发展目标

> 报告日期：2026-06-16
> 汇总依据：`reports/` 下 17 份 0615 评估文档
> 基线版本：`@actalk/inkos` v1.5.0

---

## 零、2026-06-16 实测复核补充

> 本节为对 0615 系列进度报告的当前代码复核结果。下文历史数据如与本节冲突，以本节为准。

### 0.1 当前可验证状态

| 项目 | 复核结果 | 说明 |
|------|:------:|------|
| 分支状态 | ⚠️ | 当前在 `master`，本地相对 `origin/master` ahead 22；存在未跟踪 0616 报告与 3 个 `tsconfig.tsbuildinfo` 构建缓存改动 |
| 类型检查 | ✅ | `pnpm typecheck` 已通过 |
| 构建 | ✅ | `pnpm build` 已通过；Studio 仍有 Vite chunk 体积与静态/动态混合导入警告 |
| 测试 | ✅ | `pnpm test` 退出码为 0；可见 Core 为 131 文件 / 1400 测试通过，旧报告中的测试计数需刷新 |
| sensitive-words 残余 | ✅ | `packages/` 源码未检出 `sensitive-words / SensitiveWords / 敏感词` 残余 |
| root `package-lock.json` | ✅ | 当前根目录无 `package-lock.json` |
| GitHub Actions | ❌ | 当前无 `.github/` 目录，CI/CD 仍未闭环 |
| 版本元数据 | ⚠️ | Git 历史已有 `v1.5.0` 标签，但 root/core/cli/studio `package.json` 仍为 `1.4.1` |

### 0.2 前后端闭环复核

| 模块 | 当前状态 | 判断 |
|------|:------:|------|
| Event Chain | ⚠️ | Studio 已有前端入口与 `GET/POST /books/:id/event-chain`，服务端可从 sources 生成并持久化事件链；但在 Core 生成链路中尚未证明被 Planner/Writer 作为强约束消费 |
| Scene Templates | ⚠️ | Studio 已有前端入口与 `GET/PUT /books/:id/scene-templates`，PUT 已有 Zod Schema 校验；但当前更像素材管理，还未闭环到章节规划/写作选择 |
| Voice Profiles | ✅/⚠️ | Studio 已有前端入口与分析端点，服务端会生成 `story/voice_profiles/<characterId>.json`，Writer 会读取并注入“角色声音画像”；仍需用质量评测验证其对 OOC 与对话稳定性的实际收益 |
| DistillationPage | ❌ | `DistillationPage.tsx` 存在，但未在 `App.tsx` / StyleManager 路由中挂载，属于仍未闭合的前端功能入口 |
| CLI 包边界 | ⚠️ | CLI 仍直接依赖 `@actalk/inkos-studio`；短期不一定阻断 workspace 构建，但长期会放大 CLI 发布体积与服务端耦合 |

### 0.3 对 0615 报告结论的修正

1. “Event Chain / Scene Templates / Voice Profiles 无前端”的早期结论已过时：当前 Book Workspace 已有对应 section。
2. “声纹只是桩实现”的结论已过时：当前已经有规则分析、持久化与 Writer 注入链路。
3. “全量测试 1845/1845”的数字需要重算：0616 实测以 `pnpm test` 退出码为 0 为准，Core 可见计数为 1400。
4. 项目当前主要风险已经从“代码能否跑通”转移到“质量收益能否被数据证明、前端关键入口是否可达、发布基础设施是否可复现”。

### 0.4 复核后的阶段判断

当前项目可判定为：

```
工程基线：可用
初期交付：有条件通过
Stage 5：证据不足，尚未完全通过
下一阶段核心：从功能堆叠转向质量收益证明与产品闭环
```

优先级应从继续增加 Agent 数量，调整为先证明已有 Agent 和素材系统确实提升写作质量。下一阶段最重要的不是“再加一个模块”，而是建立可重复的质量实验：同一章节、同一输入、同一评价维度下，比较开启/关闭声纹、事件链、场景模板、Beta Reader 修订后的差异。

---

## 一、当前状态总览

### 1.1 项目定位

```
从「自动生成小说的多 Agent 管线」
   → 转向「作者可介入、过程可观察、结果可校验的创作工作台」

这一转向已完成代码骨架，正进入「产品级闭环」阶段。
```

### 1.2 六模块成熟度

| 模块 | 完成度 | 测试覆盖 | 状态 |
|------|:------:|:--------:|:----:|
| Core 写作引擎 | 95% | 129 文件 / 1392 测试 ✅ | 成熟 |
| CLI 命令行工具 | 90% | 35 文件 / 176 测试 ✅ | 成熟 |
| Studio 前端工作台 | 85% | 25 文件 / 277 测试 ✅ | 良好 |
| 风格与角色系统 | 92% | 角色卡 100% / 风格 90% ✅ | 良好 |
| 数据闭环与评测 | 42% | 自动管线就绪 ⚠️ | **瓶颈** |
| 构建与基础设施 | 88% | 三包构建通过 ✅ | 需加强 |

### 1.3 交付判定

| 标准 | 状态 |
|------|:----:|
| 全量类型检查 | ✅ Core/CLI/Studio 四包零错误 |
| 全量测试 | ✅ 1845/1845 通过（100%） |
| P0 缺口 | **0** |
| P1 缺口 | **2**（非阻塞） |
| 前端页面可访问 | ✅ 16+ 路由全部正常 |
| **初期交付** | **✅ 通过** |

---

## 二、0615 关键改进回顾

### 2.1 已完成的工程改进

| 改进项 | 文件 | 影响 |
|--------|------|------|
| Writer 角色声音画像注入 | `writer.ts` | 角色对话风格一致性 ↑ |
| Auditor dim-38 角色声音偏移 | `continuity.ts` | OOC 对话可检测 |
| 第 2 个 Reader Ensemble | `calibration-results-ensemble.csv` | 跨模型交叉验证 |
| DistillationPage 类型修复 | `DistillationPage.tsx` | 消除前端类型错误 |
| 事件链深层导入统一 | `server.ts` | P0 阻断消除 |
| VoiceProfile 端点对接 | `server.ts` | 桩实现 → 真实分析 |
| SceneTemplate Schema 校验 | `server.ts` | PUT 端点增加 Zod 校验 |
| 校准集 36→50 对 | `calibration-results.csv` | 统计显著性提升 |
| CLI vitest 配置 | `packages/cli/vitest.config.ts` | 测试隔离 |
| EditorConfig | `.editorconfig` | 编辑器一致性 |

### 2.2 验证的关键数据

| 指标 | 0614 基线 | 0615 当前 | 变化 |
|------|:---------:|:---------:|:----:|
| 测试用例数 | 1392(Core)+175(CLI)+277(Studio) | 1392+176+277 | **+1**（CLI） |
| 校准集 | 36 对 | **50 对** | **+39%** |
| 版本胜率（low-temp） | 58.9% | 56.8% | 稳定 |
| 位置偏差（A/B） | A=46.0%, B=54.0% | **A=48.2%, B=51.8%** | ✅ 改善 |
| Ensemble Reader | 1 个 | **2 个** | ✅ 新增 |
| P0 缺口 | 1 | **0** | ✅ 消除 |
| P1 缺口 | 5 | **2** | ✅ 消除 3 个 |

---

## 三、推进建议

### 3.1 立即执行（24 小时内）

| 优先级 | 任务 | 工作量 |
|:------:|------|:------:|
| **P0** | CI/CD 建立（GitHub Actions）— typecheck → build → test | 2h |
| **P0** | 全量改动提交并推送至 Gitee + GitHub | 5min |
| **P1** | DistillationPage 路由集成到 StyleManager | 2h |
| **P1** | CLI → Studio 依赖声明优化（`optionalDependencies`） | 30min |

### 3.2 短期冲刺（1-2 周）

| 优先级 | 方向 | 任务 | 预期效果 |
|:------:|------|------|----------|
| **1** | **角色声音专项** | Writer prompt 注入 → Auditor dim-38 → Studio 前端 → Ensemble 复测 | character 37.5% → ≥ 50% |
| **2** | 校准数据 | 多 Reader 运行 + 人工 10 对标注 | Ensemble 可信度建立 |
| **3** | 集成测试 | LLM-stub 模式全管线 E2E 测试（+15 个） | 测试覆盖面 ↑ |
| **4** | 前端闭环 | 音频事件链/场景模板前端 | 5 个孤端点消除 |

### 3.3 中期发展（1-2 月）

```
Stage 0-4（当前） → Stage 5（质量可验证） → 多管线产品化
      │                    │                       │
      ▼                    ▼                       ▼
  代码闭环           数据闭环 + CI/CD        六类创作管线
  47 Agent 闭环      金标 20+ 对            长篇/续写/同人/
  1845 测试通过      人工评测建立            改写/短篇/适配
```

---

## 四、进一步发展目标

### 4.1 产品目标

| 阶段 | 目标 | 时间 | 验证标准 |
|------|------|:----:|----------|
| **R0** | 可发布基线 | **已完成** | `pnpm typecheck && pnpm build && pnpm test` |
| **R1** | 作者意图闭环 | 1-2 周 | 创作访谈→候选→选择→正文→核验全链路 |
| **R2** | 叙事状态闭环 | 2-4 周 | 章节正文→事件链→角色状态→下一章检索 |
| **R3** | 质量修订闭环 | 4-8 周 | 诊断→证据→修订 diff→重检→趋势 |
| **R4** | 多管线产品化 | 8-12 周 | 六类管线各有 10+ 基准案例 |

### 4.2 架构目标

```
当前架构：
  Core（47 Agent）→ Studio（140 端点）→ 前端（14 页面）
  
目标架构：
  Core（引擎层）→ API（共享契约层）→ CLI + Studio（消费层）
                           ↕
                    Zod Schema 共享类型
                    package/shared/ 目录
```

### 4.3 质量目标

| 指标 | 当前 | 1 个月目标 | 3 个月目标 |
|------|:----:|:----------:|:----------:|
| 测试通过率 | 100% | 100% | 100% |
| P0 缺口 | 0 | 0 | 0 |
| P1 缺口 | 2 | 0 | 0 |
| 校准集 | 50 对 | 80 对 | 100+ 对 |
| 金标数据集 | 0 | 20 对 | 30 对 |
| Reader 数 | 2 | 3 | 3+ |
| CI 覆盖率 | 0% | 100%（typecheck/build/test） | 100% |
| 维度覆盖 | 5 | 5（+技术质量辅助） | 20 全维度 |

---

## 五、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|------|:----:|:----:|------|
| 校准数据不足 | 中 | 高 | Ensemble 替代 + 轻量人工 10 对 |
| 多人人工评测难以组织 | 高 | 中 | Ensemble 方案已有替代路径 |
| 角色声音专项效果不达预期 | 中 | 中 | 保持候选方向：场景结构增强 |
| CI/CD 未及时建立 | 高 | 高 | 最小 CI 配置 1h 可完成 |
| 跨远端合并冲突 | 低 | 中 | 合并前检查 `--allow-unrelated-histories` |

---

## 六、结论

```
┌─────────────────────────────────────────────────────┐
│                                                     │
│  项目当前状态：代码层 92% 闭环，数据层 42% 闭环       │
│  交付判定：✅ 可初期交付                              │
│                                                     │
│  下一阶段核心：角色声音专项（2 周）                    │
│   → character 维度从 37.5% 提升至 ≥ 50%              │
│                                                     │
│  并行推进：CI/CD 建立（24h）                           │
│   → 防止已修复问题再次合入                            │
│                                                     │
│  长期方向：从 Agent 数量转向闭环质量                    │
│   → 三个写作闭环 + 六类创作管线                         │
│                                                     │
└─────────────────────────────────────────────────────┘
```
