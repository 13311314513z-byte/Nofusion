# 0620 交付目标完成度报告 KM 版

> **日期**: 2026-06-20（以当前工作区实测状态为准）  
> **当前 HEAD**: `67e1ea9`  
> **工作区状态**: 存在未提交修改（C1/C2/C3/C4 拆分及修复进行中）  
> **参考报告**: `0620全项目终审报告.md`、`0620全项目终审报告GPT.md`、`0620全项目终审报告KM.md`、`0620修改后项目当前状态技术报告GPT.md`、`0620终审+技术债重构手册完整版.md`  
> **立场**: 只认当前实测，不认报告声称；不修改代码，仅记录状态。

---

## 〇、当前门禁实测结果

| 门禁 | 结果 | 关键证据 |
|------|:--:|------|
| `pnpm -r typecheck` | ✅ 通过 | core / studio / cli 三包均通过 |
| `pnpm -r build` | ✅ 通过 | core + studio(client/server) + cli 均构建成功，`dist/index.html` 校验通过 |
| `pnpm --filter @actalk/inkos-studio build:budget` | ✅ 通过 | Dist 15350.4 KB / 15500 KB 上限（当前上限已临时放宽） |
| `pnpm -r lint` | ❌ 失败 | Studio 2 errors + 291 warnings；Core/CLI 0 errors |
| `pnpm --filter @actalk/inkos-core test` | ✅ 通过 | 135 files / 1416 tests passed |
| `pnpm --filter @actalk/inkos-studio test` | ❌ 失败 | 24 files passed / 1 failed；276 passed / 1 failed |
| `pnpm --filter @actalk/inkos test` | ❌ 失败 | 33 files passed / 1 failed；167 passed / 2 failed |

**一句话判定**: 当前代码已通过编译与构建门禁，发布包体预算可过；但 lint 与测试（Studio 1 项、CLI 2 项）仍未全绿，尚不能视为 0620 交付目标完全达成。

---

## 一、关键文件规模（当前工作区）

| 文件 | 当前行数 | 0620 报告基线 | 判断 |
|------|---:|---:|------|
| `packages/studio/src/api/server.ts` | 1726 | 4084 | ✅ C1 路由拆分大幅瘦身 |
| `packages/studio/src/api/routes/agent.ts` | 339 | 内联约 800 行 | ✅ C4 agent endpoint 已外提 |
| `packages/studio/src/api/routes/style.ts` | 1921 | 1766 | ⚠️ 仍是超大业务路由，未进一步拆分 |
| `packages/core/src/pipeline/runner.ts` | 3593 | 4070 | ⚠️ C2 仅局部抽离，未达 thin facade |
| `packages/core/src/pipeline/pipeline-foundation.ts` | ~452 | — | 🟡 已抽出 foundation 逻辑 |
| `packages/studio/src/pages/StyleManager.tsx` | 225 | ~1900 | ✅ C3 主组件瘦身 |
| `packages/studio/src/pages/style-manager/useStyleManagerState.ts` | 398 | — | 🟡 状态已下沉，但 hook 过重且 lint 报错 |

---

## 二、各交付目标完成度判定

### 1. C1 / P2-1：Studio API server.ts 路由拆分

**状态**: 结构基本完成，验证未完全闭合。

- `server.ts` 已从 4084 行压到 1726 行，内联 API 路由已大量外提。
- 当前已注册路由模块：`events`、`daemon`、`cover`、`project`、`logs`、`genres`、`analytics`、`health`、`truth-browser`、`language`、`model-overrides`、`notify`、`sources`、`hooks`、`books`、`services`、`chapters`、`audit`、`style`、`runtime-truth`、`voices-scene`、`sessions`、`roles`、`detect`、`writing`、`revision-export`、`fanfic-radar-doctor`、`agent`。
- `POST /api/v1/agent` 已迁移到 `routes/agent.ts`。
- **未闭合点**:
  - `routes/style.ts` 仍 1921 行，style/import/distillation/event-chain 等大量端点仍集中。
  - `routes/writing.ts` 等也可能偏大。

### 2. C4 / P1-3：Agent endpoint 抽离

**状态**: 结构完成，存在 1 个测试回归。

- `routes/agent.ts` 339 行，已承载 `POST /api/v1/agent` 完整逻辑。
- `tryHandleExternalChatEdit` 已通过 `shared/agent-helpers.ts` 导入并真实调用（0620 GPT 报告提到的本地 stub 问题在当前工作区已不存在）。
- `loadStudioBookListSummary` 已迁移到 `shared/book-helpers.ts`。
- **未闭合点**:
  - `server.test.ts` 中 `probes the upstream when the agent returns empty text and surfaces the real error` 失败：期望返回 502 / `AGENT_EMPTY_RESPONSE`，实际返回 500 / `AGENT_ERROR`。
  - 根因：`agent.ts` 在 `responseText` 为空时尝试 fallback `chatCompletion`，但未单独捕获其异常并包装为 502，导致异常进入外层 catch 返回 500。

### 3. C3 / P2-5：StyleManager 拆分

**状态**: 前端主组件瘦身有效，但 lint 未闭合。

- `StyleManager.tsx` 已降至 225 行。
- `useStyleManagerState.ts` 承担状态与 API 调用。
- `StyleDiagnoseTab.tsx` 已缩小。
- **未闭合点**:
  - `useStyleManagerState.ts` 第 247、280 行使用 `// eslint-disable-next-line react-hooks/exhaustive-deps`，但项目未启用该 eslint 规则，导致 lint **2 errors**。
  - 该文件仍有 `any` 警告与未使用变量/参数警告。
  - `StyleImportTab` 大 props 面与未使用 props 问题仍存在。

### 4. C2 / P2-2：PipelineRunner 拆分

**状态**: 局部拆分，仍有较重单体债。

- `runner.ts` 仍 3593 行，不是 thin facade。
- `pipeline-foundation.ts` 已抽出约 452 行 foundation 逻辑。
- `PipelineContext`、`PipelinePool` 已落地。
- **当前实测**: Core 1416 项测试全绿，说明当前 C2 改动未引入 Core 回归（至少在本轮 core 测试范围内）。
- **未闭合点**: `runner.ts` 仍过大，需继续拆分。

### 5. C7 / P2-7：Pipeline 实例池

**状态**: 已接入，当前未破坏 Core 测试；Studio 剩余失败与 pool 无直接关联。

- `PipelinePool` 已从 core export，Studio `shared/pipeline.ts` 引入。
- 0620 GPT 报告提到的 111 个 Studio 测试失败在当前工作区已不复存在。
- **当前实测**: Core 全绿；Studio 唯一失败是 agent empty-response 状态码回归。

### 6. P2-4：use-api.ts 双模式 / 共享 API utils

**状态**: 当前未观察到 server/client 边界编译错误。

- 0620 GPT 报告提到的 `api-utils.ts` 引用 `window` 导致 server build 失败的问题，在当前工作区 `pnpm -r build` 已通过，说明该问题已被修复或路径已调整。
- `use-api.ts` 仍存在动态/静态混用提示（Vite 构建时警告），影响拆包效果，但不再阻断构建。

### 7. P2-13：Bundle CI / build:budget

**状态**: 通过，但依赖临时放宽预算上限。

- 当前 Dist 15350.4 KB / 15500 KB 上限，通过。
- 0620 原始上限 15000 KB 仍会被突破；当前通过是因为上限已临时调整为 15500 KB。
- 体积热点：`vendor-streamdown` 1616 KB、`index` 835 KB、`emacs-lisp` / `cpp` / `wasm` / `wardley` 等 language packs。

### 8. 25 项技术债修复（0620 终审报告口径）

结合 `0620全项目终审报告KM.md` 与 `0620终审+技术债重构手册完整版.md` 的交叉结论，当前状态如下：

| 类别 | 数量 | 说明 |
|------|:--:|------|
| 代码确认 + 当前门禁通过 | 约 19 项 | P0-1、P0-4、P0-5、P1-1、P1-2、P1-5、P1-6、P1-7、P1-8、P1-9、P1-10、P1-11、P1-14、P1-15、P2-6、P2-9、P2-10、P2-11、P2-13（脚本存在且 budget 通过） |
| 部分修复 / 未完全接入 | 4 项 | P0-7（LLM 解析加固有凑数）、P1-8（去重代码存在但 lint 报 unused）、P1-13（仅 beta-reader 使用 AgentContext 温度）、P2-13（budget 通过但依赖放宽上限） |
| 虚报 / 当前仍应判未修复 | 2 项 | P0-2（StateManager 生产代码仍 40+ 处实例化，非仅 1 处）、P1-12（writer.ts `dialogueRegex` 仍为局部常量，无 regex cache） |
| 延后架构重构 | 15 项 | P1-3、P1-4、P1-16、P2-1~P2-5、P2-7、P2-12、P2-14~P2-18 |
| 主动跳过 / 合法豁免 | 3 项 | P0-6 API key 明文、2 项 eslint no-control-regex |

> 注：以上数字与 `0620终审+技术债重构手册完整版.md` 基本一致；差异在于当前实测 build/budget/typecheck 已通过，而 GPT 版报告记录的是未修复前的失败状态。

---

## 三、当前各模块问题清单

### 3.1 Studio 模块

| 问题 | 位置 | 严重度 | 说明 |
|------|------|:--:|------|
| lint 硬失败 | `useStyleManagerState.ts:247,280` | 🔴 高 | `react-hooks/exhaustive-deps` disable 注释触发 error；门禁未过。 |
| 测试回归 | `server.test.ts:3562` | 🔴 高 | agent 空响应 upstream 探测应返回 502，实际返回 500。 |
| 超大路由 | `routes/style.ts` | 🟡 中 | 1921 行，业务混合度高。 |
| 路由注册完整性 | `server.ts` | 🟢 低 | 静态扫描 `/api/v1` 路由无重复，主文件无内联 API 路由。 |
| use-api 混用 | `hooks/use-api.ts` | 🟡 中 | Vite 警告动态/静态导入混用，影响拆包收益但不阻断构建。 |

### 3.2 Core 模块

| 问题 | 位置 | 严重度 | 说明 |
|------|------|:--:|------|
| runner.ts 单体 | `pipeline/runner.ts` | 🟡 中 | 3593 行，拆分仅完成局部。 |
| StateManager 复用虚报 | `core/src/agent/agent-tools.ts`、`pipeline/*.ts` 等 | 🟡 中 | 生产代码多次 `new StateManager`，非全局唯一。 |
| regex cache 未实现 | `agents/writer.ts` | 🟡 中 | `dialogueRegex` 仍为局部常量。 |
| 当前实测 | 全部 Core 测试 | 🟢 低 | 1416 tests green，本轮未引入回归。 |

### 3.3 CLI 模块

| 问题 | 位置 | 严重度 | 说明 |
|------|------|:--:|------|
| integration 测试失败 | `src/__tests__/cli-integration.test.ts` | 🟡 中 | 2 项测试在运行时找不到 `dist/index.js` / `dist/runtime-requirements.js`，可能与测试/构建时序或环境有关；`pnpm -r build` 已生成 dist。 |
| typecheck / build | 全包 | 🟢 低 | 当前已通过。 |

---

## 四、与 0620 交付目标的差距

| 目标 | 0620 报告声称 | 当前实测 | 差距 |
|------|:--:|:--:|------|
| typecheck 全绿 | ✅ | ✅ | 无差距 |
| build 全绿 | ❌（GPT 版） | ✅ | 已修复 |
| build:budget 通过 | ❌（GPT 版） | ✅（依赖放宽上限） | 名义通过，仍需体积优化 |
| lint 全绿 | ✅（DS 版） | ❌ | 2 errors 未修复 |
| Core tests 全绿 | ✅ | ✅ | 无差距 |
| Studio tests 全绿 | — | ❌ | 1 项 agent 状态码回归 |
| CLI tests 全绿 | — | ❌ | 2 项 integration 失败 |
| server.ts < 2000 行 | — | ✅ | 已达成 |
| agent endpoint 外提 | — | ✅ | 已达成 |
| StyleManager 拆分 | — | 🟡 | 组件已拆，lint 未闭合 |
| runner.ts 拆分 | — | ❌ | 仍 3593 行 |

---

## 五、结论

当前项目处于 **“重构形态已落地、编译构建已恢复、但 lint 与测试尚未完全闭合”** 的状态。

- ✅ **已达成**: `typecheck`、`build`、`build:budget`、Core 全量测试；`server.ts`、`agent.ts`、`StyleManager.tsx` 规模目标达成。
- ❌ **未达成**: `lint`（2 errors）、Studio 1 项测试回归、CLI 2 项 integration 失败、`runner.ts` 仍偏大、`routes/style.ts` 仍为大单体。
- 🟡 **虚报项仍需关注**: P0-2 StateManager 复用、P1-12 regex cache，当前代码仍不支持原报告声称的闭合状态。

**0620 交付目标完成度**: 若以“编译、构建、Core 测试、主文件规模目标”为口径，完成度约 **75%~80%**；若以“lint 全绿 + 全量测试全绿 + 大文件拆分全部到位”为口径，完成度约 **60%~65%**。

**建议下一步**:
1. 修复 `useStyleManagerState.ts` 的 eslint disable 注释，或启用/配置对应 rule。
2. 修复 `agent.ts` 空响应 fallback 的异常捕获，返回 502 / `AGENT_EMPTY_RESPONSE`。
3. 调查 CLI integration 测试的 dist 路径问题（可能是并发 build 覆盖或测试隔离问题）。
4. 继续推进 `runner.ts` 与 `routes/style.ts` 的拆分。
5. 重新审计 P0-2 / P1-12 等虚报项，避免报告与代码再次脱节。

---

> 本报告基于当前工作区实测状态生成，未修改任何项目文件与代码。
